{
  "name": "program-a-canvas-foundation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && npm --prefix program-c run typecheck"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^7.3.6"
  }
}
